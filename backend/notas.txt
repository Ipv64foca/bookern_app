Usuarios
---------
id (PK)
nombre
apellido
email (único)
password_hash
rol_id (FK -> Roles.id)
fecha_registro
pais
idioma
foto_perfil_url
activo (boolean)

Roles
---------
id (PK)
nombre (admin, profesional, cliente)

Profesionales
---------
id (PK)
usuario_id (FK -> Usuarios.id)
especialidad (nutricionista, fitness_coach, etc.)
biografia
experiencia
precio_base
stripe_account_id
verificado (boolean)

Planes
---------
id (PK)
profesional_id (FK -> Profesionales.id)
nombre
descripcion
precio
frecuencia (mensual, semanal)
beneficios (JSON)
activo (boolean)

Suscripciones
---------
id (PK)
cliente_id (FK -> Usuarios.id)
plan_id (FK -> Planes.id)
fecha_inicio
fecha_fin
estado (activa, cancelada, vencida)
stripe_subscription_id

Citas
---------
id (PK)
suscripcion_id (FK -> Suscripciones.id)
fecha
hora_inicio
hora_fin
estado (pendiente, confirmada, cancelada, completada)
enlace_video (opcional)

Mensajes
---------
id (PK)
chat_id (FK -> Chats.id)
remitente_id (FK -> Usuarios.id)
contenido
fecha_envio
tipo (texto, archivo)
leido (boolean)

Chats
---------
id (PK)
suscripcion_id (FK -> Suscripciones.id)
ultimo_mensaje
fecha_ultimo_mensaje

Archivos
---------
id (PK)
profesional_id (FK -> Profesionales.id)
titulo
descripcion
tipo (pdf, video, link)
url
acceso_minimo_plan_id (FK -> Planes.id)
fecha_subida
